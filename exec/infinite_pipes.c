#include "../inc/header.h"

void    loop_pipe()
{
    
}

void pipe-loop{
  int *end[2];

  while(cmd->next)
  {
    pipe(ends);
    int pid=fork();
    if (pid ==0)
    {
      child(cmd);
    }
    cmd = cmd->next;
  }
  execute-lastcmd();
}
